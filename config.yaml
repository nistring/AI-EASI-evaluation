num_workers: 16

train:
  max_epochs: 500
  batch_size: 32
  lr: 1.0e-5
  weight_decay: 0
  gamma: 0.99
  loss_kwargs:
    type: "elbo"
    kappa: 0.60
    rate: 1.0e-2
    alpha: 5.0e+2
    beta: 1

test:
  mc_n: 30
  batch_size: 16

predict:
  metric: "std"
  th: 1.0
  step: 192

model:
  latent_dims: [1,1,1,1]
  in_channels: 3
  num_classes: 4
  num_cuts: 3
